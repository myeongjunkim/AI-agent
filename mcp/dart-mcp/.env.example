# ============================================
# DART MCP 환경변수 설정
# ============================================
# 이 파일을 .env로 복사하고 실제 값을 입력하세요:
# cp .env.example .env

# ============================================
# 필수 설정
# ============================================

# DART API Key (필수)
# https://opendart.fss.or.kr/ 에서 발급받은 API 키
DART_API_KEY=your-dart-api-key-here

# ============================================
# 메인 LLM 설정 (문서 분석, 요약 등에 사용)
# ============================================

# LLM Provider (openai, vllm, none)
LLM_PROVIDER=openai

# OpenAI API 사용시 (LLM_PROVIDER=openai)
OPENAI_API_KEY=your-openai-api-key-here
LLM_MODEL=gpt-3.5-turbo
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=1000

# vLLM 서버 사용시 (LLM_PROVIDER=vllm)
# LLM_BASE_URL=http://localhost:8000/v1
# LLM_MODEL=meta-llama/Llama-3.1-8B-Instruct
# LLM_API_KEY=dummy-key  # vLLM 서버가 인증을 요구하는 경우

# ============================================
# LangExtract/쿼리 파싱 LLM 설정
# ============================================
# 자연어 쿼리에서 정보 추출 전용 LLM 설정
# 메인 LLM과 다른 모델 사용 가능 (예: 빠른 추출용 경량 모델)

# 추출용 LLM Provider 선택
# Option 1: vLLM Server (로컬 LLM, 빠른 응답)
EXTRACTION_USE_VLLM=false
EXTRACTION_VLLM_BASE_URL=http://localhost:8000/v1
EXTRACTION_VLLM_API_KEY=dummy-key
EXTRACTION_VLLM_MODEL_NAME=meta-llama/Llama-3.1-8B-Instruct

# Option 2: LangExtract with Gemini (Google AI Studio)
# https://aistudio.google.com/apikey 에서 API 키 발급
# EXTRACTION_USE_LANGEXTRACT=true
# LANGEXTRACT_API_KEY=your-gemini-api-key-here
# LANGEXTRACT_MODEL=gemini-2.0-flash-exp

# Option 3: 메인 OpenAI 설정 재사용
# EXTRACTION_USE_MAIN_LLM=true  # 위의 OPENAI_API_KEY 사용

# Option 4: Ollama (로컬 실행)
# EXTRACTION_USE_OLLAMA=true
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.1:8b

# ============================================
# 캐시 설정
# ============================================

# 캐시 디렉토리
DART_CACHE_PATH=./cache/dart

# 캐시 TTL (시간 단위)
DART_CACHE_TTL=24

# ============================================
# API 제한 설정
# ============================================

# DART API 요청 제한 (일일 최대)
DART_API_RATE_LIMIT=1000

# 최대 검색 결과 수
DART_MAX_SEARCH_RESULTS=100

# ============================================
# 처리 설정
# ============================================

# 병렬 다운로드 수
DART_PARALLEL_DOWNLOADS=5

# 파싱 타임아웃 (밀리초)
DART_PARSE_TIMEOUT=30000

# 다운로드 경로
DART_DOWNLOAD_PATH=./downloads/dart

# ============================================
# 로깅 설정
# ============================================

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 로그 디렉토리
LOG_DIR=./logs

# ============================================
# 추가 설정 (선택적)
# ============================================

# 설정 파일 경로 (JSON 형식)
# DART_CONFIG_PATH=./config.json